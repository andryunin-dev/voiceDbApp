1) перед удалением проверить на использование, чтобы избежать вываливания ошибок - сделано
2) валидация санитация.
3) сделать в валидаторе модели возможность использования yield (наверное в фреймворке нужно проверить, что вернул валидатор, если Exception, то бросить его).
4) посмотреть конструктор Multiexception. По умолчанию в нем прописывается класс \T4\Core\Exception. Может лучше прописать \Exception? Тогда в коде можно будет бросать стандартные исключения (\Exception).
5) в валидаторе платформы, модуля и т.д. сравнивается на совпадение только название платформы. Надо бы сделать сравнение с учетом вендора
6) то же самое, что и п.5, но в экшенах редактирования
7) подсветка текущего пункта в меню сделана через $this->data->activeLink в экшене. Как то не очень нравится
8) у платформы устройства пока не задействован инвентарный номер

!!! подключить popup OfficeDetail по клику на строке таблицы офисов.
!!! делать страницу devices (сделал добавление устройств, сделать их вывод в виде инвентори(без портов, с модулями) и порты(без модулей, с портами))
!!! доделать editDataPort, чтобы давал изменить только маску сети

в твиге сделать функцию:
func tree(currentId)
{
	искать детей currentId в childrenList
	if childrenList->count > 0 (currentId имеет детей) {
		вставка верхней части шаблона нового уровня(li > ul)
		foreach childrenList as child {
			tree(child->id);
		}
		вставка нижней части нового уровня (/ul /li)
	} else (currentId не имеет детей) {
		вставка leaf (li)
	}
}
    
    